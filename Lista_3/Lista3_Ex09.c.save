#include <stdio.h>
#include <string.h>

int main(){

    char numeros[50], contrario, caractere;
    int i = 0;

    printf("Digite algo para inverter: ");

    do{
        caractere = getchar();
        numeros[i] = caractere;
        ++i;

    }while(caractere != '\n');

    numeros[i - 1] = '\0';

    int num = contarTamanho(numeros);

    for(i = 0; i < num; i++){

        contrario = letras[i];
        numeros[i] = letras[num];
        numeros[num] = contrario;

        --num;
    }

    printf("\nO resultado foi: %s\n", letras);

    return 0;
}

int contarTamanho(char string[]){

    int tamanho = 0;
    while(string[tamanho] != '\0'){
        ++tamanho;
    }
    --tamanho;

    return tamanho;
}

